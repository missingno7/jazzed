# Local Game Data

Put your local Jazz Jackrabbit 1 DOS data files in this directory when running Jazzed locally.

The game data itself is ignored by git and must not be committed. The public repository should only keep this placeholder README.
